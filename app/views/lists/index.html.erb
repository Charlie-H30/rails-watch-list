<div class="navbar-lewagon d-flex justify-content-between align-items-center px-5">
  <div class="my-2">
    <button class="btn btn-flat"->Your List</button>
  </div>
  <div class="my-2">
    <img class="avatar-large sharp-image" alt="avatar-large" src="https://kitt.lewagon.com/placeholder/users/arthur-littm" />
  </div>
  <div class="d-flex my-2">
    <%= link_to "Add new list", new_list_path, class: "btn btn-ghost" %>
  </div>
</div>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3),rgba(0,0,0,0.3)), url();">
  <div class="container">
    <h1>Your Lists</h1>
    <p>Cheeky</p>
    <a class="btn btn-flat" href="#">Apply now</a>
  </div>
</div>


<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<%= link_to "Add new list", new_list_path %>
